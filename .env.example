# SQL Server SA Password
# This should be a strong password that meets SQL Server requirements
SQL_SA_PASSWORD=YourStrong1Passw0rd

# Optional: Override SQL Server host (auto-detected by default)
# SQL_SERVER_HOST=localhost              # For local development
# SQL_SERVER_HOST=weatherapp-database    # For container/devcontainer development